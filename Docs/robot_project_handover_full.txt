
FULL PROJECT HANDOVER (DETAILED)

SYSTEM OVERVIEW:
- STM32 balancing robot with ODrive (CAN)
- EKF provides:
  pitch_rad
  pitch_rate_rad_s
  wheel_vel_avg
  wheel_pos_avg
- Control loop: ControlTorqueStep()
- Modes:
  IDLE
  CATCH (recovery)
  BAL (stabilization)

---------------------------------------
ARCHITECTURE (INTENDED)

CATCH:
- PD on pitch
- + damping from wheel velocity (V term)
- goal: catch robot from tilt WITHOUT runaway

BAL:
- PD + wheel velocity term + sync
- goal: stable upright control

---------------------------------------
CRITICAL BUG HISTORY

1. V TERM LOST MULTIPLE TIMES
- V=0 in logs repeatedly
- caused by:
  - wrong variable (wheel_vel_avg undefined)
  - wrong source (should be snap.wheel_vel_avg)
  - bad replace → snap.snap.wheel_vel_avg
  - code inserted in wrong function

IMPACT:
- no damping → base runaway

---------------------------------------

2. DEBUG / CONTROL MIXED
- Control code accidentally inserted into DebugTelemetryStep
- variables missing:
  u_cmd, left_cmd, right_cmd, pitch_rate

IMPACT:
- compilation errors
- corrupted logic

---------------------------------------

3. STRING / LOG ISSUES
- missing \r\n → logs unreadable
- required:
  "...Ks=%ld\r\n"

---------------------------------------

4. MODE SWITCH PROBLEM
- CATCH → BAL triggered too early

Observed:
- high pitch_rate
- high wheel velocity
- unstable entry into BAL

Fix attempted:
#define CATCH2BAL_RATE_TH_rads 0.80 → 0.50 → 0.30

---------------------------------------

5. BASE RUNAWAY (WPR)
Observed:
- wpr grows 0 → 300+

After damping:
- reduced to ~100

Root cause:
- missing or weak V term

---------------------------------------

6. TORQUE SATURATION
Observed:
- u hits limit (sat=1)
- robot cannot recover

Tested:
CATCH_U_LIMIT:
0.35 → 0.40 → 0.45 → 0.50 (planned)

---------------------------------------

7. FALL PROTECTION FAILURE (CRITICAL)

Expected:
- disable motors when robot falls

Implemented:
fabs(pitch) > 1.2
then asymmetric:
pitch > 1.2
pitch < -1.6

BUT:
- did NOT trigger
- robot fell and continued spinning wheels

Conclusion:
❗ safety logic broken or placed incorrectly

---------------------------------------

8. BAD PATCHING PROCESS
- multiple global replace operations
- broke:
  variable names
  logic placement
  structure

Conclusion:
❗ must rebuild from clean base

---------------------------------------

TESTED CONSTANT SETS

BAL:
CONTROL_K_PITCH = 0.8 → 0.55
CONTROL_K_PITCH_RATE = 0.45
CONTROL_K_WHEEL_VEL = 0.2 → 0.1
CONTROL_PITCH_TRIM_RAD = 0.18–0.20

CATCH:
CATCH_K_PITCH = 0.65 → 0.55 → 0.45
CATCH_K_PITCH_RATE = 0.45
CATCH_U_LIMIT = 0.35 → 0.45

MODE:
CATCH2BAL_PITCH_TH_rad = 0.04 → 0.08
CATCH2BAL_RATE_TH_rads = 0.80 → 0.30
BAL2CATCH_PITCH_TH_rad = 0.16 → 0.20
CATCH_HOLD_MS = 200

DAMPING:
0.05 → 0.15 (working level)

---------------------------------------

CURRENT STATE (IMPORTANT)

From latest logs:
- robot catches partially
- base moves (wpr ~250)
- transitions to BAL too early
- fails recovery
- falls
- motors KEEP spinning (FATAL ISSUE)

---------------------------------------

KEY LESSONS

1. Damping (V) is mandatory in CATCH
2. Mode switching must consider rate, not only angle
3. Torque limit must be sufficient
4. Safety cutoff must be first in loop
5. Debug must reflect real control (same signals)

---------------------------------------

NEXT CHAT PLAN

1. CLEAN RESET
- restore working main.c
- no patch accumulation

2. REBUILD CONTROL STEP:
- clear CATCH block
- clear BAL block

3. FIRST PRIORITY:
❗ implement reliable FALL cutoff

4. SECOND:
- ensure V term always active

5. THIRD:
- fix mode switching

6. ONLY THEN:
- retune gains

---------------------------------------

RULES

- ONE CHANGE PER ITERATION
- VERIFY USING LOGS ONLY
- NO MASS REPLACE
- ALWAYS KEEP CODE CONSISTENT

